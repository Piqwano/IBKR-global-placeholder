# Global RSI Bot v2.3 — Production requirements
# ══════════════════════════════════════════════════════════════════════════
# Only dependencies needed for the live bot.
# yfinance is intentionally excluded — it's a backtest-only dependency.

ib_insync==0.9.86
numpy>=1.26,<2.0
pandas>=2.1,<3.0
requests>=2.31
tzdata

# Dashboard (FastAPI + ASGI)
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
